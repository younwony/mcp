spring:
  application:
    name: mcp-server
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

server:
  port: 8080

logging:
  level:
    root: INFO
    com.example.mcp: DEBUG
